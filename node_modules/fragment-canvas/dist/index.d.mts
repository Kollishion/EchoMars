type FragmentCanvasOptions = {
    /**
     * Fragment shader to use.
     */
    fragmentShader: string;
    /**
     * Vertex shader to use (you probably don't need this).
     */
    vertexShader: string;
    /**
     * Custom uniforms to pass to the shader.
     * Use `gl.uniform[1234][fi][v](location, [value])` to set their values.
     */
    uniforms: Record<string, (gl: WebGLRenderingContext, location: WebGLUniformLocation, time: DOMHighResTimeStamp) => void>;
    /**
     * Whether to automatically render the shader with requestAnimationFrame.
     * @default true
     */
    autoRender: boolean;
    /**
     * Blend function to use
     *
     * @default [WebGLRenderingContext.SRC_ALPHA, WebGLRenderingContext.ONE]
     */
    blendFunc: [number, number];
};
declare class FragmentCanvas {
    canvas: HTMLCanvasElement;
    options: FragmentCanvasOptions;
    gl: WebGLRenderingContext;
    shaderProgram: WebGLProgram;
    uniformLocations: Record<string, WebGLUniformLocation | null>;
    constructor(canvas: HTMLCanvasElement, options?: Partial<FragmentCanvasOptions>);
    render(time: DOMHighResTimeStamp): void;
    private addShaderToProgram;
}

export { FragmentCanvasOptions, FragmentCanvas as default };
